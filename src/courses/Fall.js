import React from 'react-dom';

function Fall() {
    return (
        <div>
            <p>Please select a term for which courses have already been added.</p>
        </div>
    );
}

export default Fall;